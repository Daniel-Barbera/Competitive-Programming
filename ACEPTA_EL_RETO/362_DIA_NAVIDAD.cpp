#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int a, b;
    int numCasos;
    cin >> numCasos;
    while (numCasos--) {
        cin >> a >> b;
        a == 25 && b == 12 ? cout << "SI\n" : cout << "NO\n";
    }

}