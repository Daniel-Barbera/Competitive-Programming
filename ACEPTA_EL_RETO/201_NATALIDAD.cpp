#include <bits/stdc++.h>
using namespace std;

int main() {
    string input; 
    while (getline(cin, input) && input != ".") {
        int children = 0, current = 0; 
        for (int i = 0; i < input.size(); i+= 2;) {
            
        }
    }
}