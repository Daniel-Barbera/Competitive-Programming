#include <bits/stdc++.h>
using namespace std;

int main() {    
    constexpr int size = 2*10*10*10*10+1;
    int numCases, n, s; 
    scanf("%d", &numCases);
    vector<int> sequence(size);
    while (numCases--) {
        scanf("%d %d", &n, &s);
        
    }
    return 0;
}