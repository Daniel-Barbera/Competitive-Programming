#include <iostream>
using namespace std;

int main() {
	
	int numTests = 0;
	long long int m, n = 0;
	cin >> numTests;
	for (int i = 0; i < numTests; ++i) {
		cin >> m;
		cin >> n;
		for (int j = m; j <= n; ++j) {
			for (int k = j; j = 1; --j) {
				
			}
		}
	}
	
	return 0;
}