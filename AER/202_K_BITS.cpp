#include <bits/stdc++.h>
using namespace std;

int main() {
    int numDigitos, numUnos, numCeros;
    while (cin >> numDigitos >> numUnos && numDigitos && numUnos) {
        numCeros = numDigitos - numUnos;
    }
    return 0;
}